# Frontend'ni Python backend dashboard'iga ulaydi.
# Bu o'rnatilsa, panel mock rejim o'rniga haqiqiy WebSocket backend'ga ulanadi
# (python/dashboard/server.py — ws://localhost:8765).
# Backend ishlamasa, panel avtomatik mock rejimga qaytadi (xavfsiz fallback).
VITE_WS_URL=ws://localhost:8765
VITE_HOTELOS_TOKEN=hotel2024
# REST API (FastAPI — python/main.py) — commented out to keep mock mode default.
# Uncomment when backend is running:
# VITE_API_URL=http://localhost:8000
